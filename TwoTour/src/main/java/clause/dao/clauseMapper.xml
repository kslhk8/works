<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
 PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 <mapper namespace="clauseSQL">
	<select id="getClause" parameterType="String" resultType="clause">
		select * from 약관및참고사항 where arrive_city=#{arrive_city}
	</select>
 </mapper>